[project]
name = "wenyi"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
  "beautifulsoup4>=4.12",
  "openai>=3.3.1",
  "pydantic>=2.0",
  "pyyaml>=6.0",
]

[dependency-groups]
dev = ["pytest>=8.0", "ruff>=0.12"]

[tool.pytest.ini_options]
pythonpath = ["."]
