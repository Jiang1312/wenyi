## 当前工作目标

- Wenyi 是基于 LLM Agent 的长篇翻译项目。
- 已确认后续方向是用边界明确的 Agent Loop 重构后端，替代固定 Pipeline 中适合由
  模型动态决策的部分；确定性任务仍由普通代码控制。
- Translation Loop MVP 已完成可行性实验。当前处于架构设计阶段，目标是梳理现有后端
  必须保留的能力与约束，明确开放问题、设计选择、实施顺序和验证方式。
- 在设计达成共识前，不预设具体接口、类、持久化格式或 Tool Calling 实现，也不开始
  修改正式流程。
- 当前仍只在 `loop-lab/` 中进行实验和设计准备，不修改 `trans_novel/` 正式流程。
- 开始实验任务前，先阅读 `loop-lab/PLAN.md`。
